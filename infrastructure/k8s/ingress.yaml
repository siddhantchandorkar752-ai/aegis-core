apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: neuro-core-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: neuro-core.cloud.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: neuro-core-service
            port:
              number: 80
